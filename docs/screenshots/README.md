# Dashboard Screenshots

Add the following images here so they render in the main `README.md`:

| Filename | Capture |
|----------|---------|
| `dashboard.png` | Full SOC dashboard — KPI cards, live attack feed, charts, high-risk alerts. |
| `attack-detail.png` | Attack/session detail modal showing commands, behaviours, MITRE techniques, and the timeline. |
| `incidents.png` | Incident cards view (severity + status). |
| `report.png` | A generated Executive PDF report. |

**Tips for clean captures**
- Run with `DEMO_MODE=true` so the dashboard is fully populated.
- Use a wide browser window (≥1440px) and the dark SOC theme.
- Crop to the content; PNG; keep each under ~500 KB if possible.
